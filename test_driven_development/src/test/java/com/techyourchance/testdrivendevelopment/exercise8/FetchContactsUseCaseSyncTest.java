package com.techyourchance.testdrivendevelopment.exercise8;

import org.junit.runner.RunWith;
import org.mockito.junit.MockitoJUnitRunner;

@RunWith(MockitoJUnitRunner.class)
public class FetchContactsUseCaseSyncTest {

	// parameter passed correctly to endpoint
	//If the server request completes successfully, then registered listeners should be notified with correct data.
	//If the server request completes successfully, then unregistered listeners should be notified.
	// If the server request fails for any reason except network error, then registered listeners should be notified about a failure.
	//If the server request fails due to network error, then registered listeners should be notified about a network error specifically.

}